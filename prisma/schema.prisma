generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Idea {
    id        String   @id @default(cuid())
    text      String
    userId    String?
    createdAt DateTime @default(now())
}
